<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="alertDialogTheme">@style/AlertDialogTheme</item>-->
    </style>

    <style name="AppThemeX" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorDeepO</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!--buttons color-->
        <item name="colorAccent">@color/colorDeepO</item>
        <!--title and message color-->
        <item name="android:textColorPrimary">@color/colorDeepO</item>
        <!--dialog background-->
        <item name="android:windowBackground">@drawable/dialog_color</item>
    </style>

    <style name="MyDialogStatusTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!--item RadioButton or CheckBox color-->
        <item name="colorControlNormal">@color/colorDeepO</item>
        <item name="colorControlActivated">@color/colorDeepO</item>
        <!--item text color-->
        <item name="textColorAlertDialogListItem">@color/colorDeepO</item>
        <!--buttons color-->
        <item name="colorAccent">@color/colorDeepO</item>
        <!--title and message color-->
        <item name="android:textColorPrimary">@color/colorDeepO</item>
        <!--dialog background-->
        <item name="android:windowBackground">@drawable/dialog_color</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
